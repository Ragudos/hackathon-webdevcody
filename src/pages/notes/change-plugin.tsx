import React from "react";


type Props = {
  currentTitle: string,
  incomingTitle: string,
  setTitle: React.Dispatch<React.SetStateAction<string>>
}

export const ChangePlugin: React.FC<Props> = ({
  currentTitle,
  incomingTitle,
  setTitle
}): null => {
  
  React.useEffect(() => {

  });

  return null;
};
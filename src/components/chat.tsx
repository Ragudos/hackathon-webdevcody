import React from "react";

export const Chat = () => {
  const [myPresence, othersPresence, updateMyPresence] = usePresence(
    userId,
    roomId,
    initialData
  );


  return (
    <React.Fragment>

    </React.Fragment>
  );
};